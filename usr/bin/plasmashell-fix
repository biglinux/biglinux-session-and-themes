#!/bin/sh

# if [ -e "/tmp/big_language" ]; then
#     export LANG=$(cat /tmp/big_language)
# fi

if [ "$(qdbus org.kde.ksmserver /KSMServer org.kde.KSMServerInterface.isShuttingDown)" = "false" ]; then

    if [ "$(cat "$HOME/.big_performance")" != "0" ] && [ "$(cat "$HOME/.big_performance")" != "" ]; then
        QT_QUICK_BACKEND=software QSG_RENDER_LOOP=basic plasmashell
    else
        QT_QUICK_BACKEND="" QSG_RENDER_LOOP="" plasmashell
    fi

fi
